services:
  backend:
    build: ./backend
    image: gigpulsesentinel/backend:latest
    container_name: gigpulsesentinel_backend
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./gigpulsesentinel.db
      SECRET_KEY: ${SECRET_KEY:-dev-secret-key-change-in-production}
      JWT_SECRET: ${JWT_SECRET:-dev-jwt-secret-change-in-production}
      JWT_ALGORITHM: HS256
      JWT_EXPIRY_HOURS: 24
      USE_MOCK_APIS: "true"
      DEBUG: "true"
      CORS_ORIGINS: http://localhost:5173,http://localhost:3000
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_STARTTLS: ${SMTP_STARTTLS:-true}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
      - backend_data:/app/backend/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build: ./frontend
    image: gigpulsesentinel/frontend:latest
    container_name: gigpulsesentinel_frontend
    environment:
      VITE_API_BASE_URL: http://localhost:8000
      VITE_APP_NAME: GigPulse Sentinel
    ports:
      - "5173:5173"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

volumes:
  backend_data:
    external: true
    name: gigpulsesentinel_backend_data
