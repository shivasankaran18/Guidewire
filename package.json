{
  "name": "gigpulsesentinel",
  "version": "1.0.0",
  "description": "An AI-Powered Parametric Income Agent for Gig Workers",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "source backend/venv/bin/activate && uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build",
    "seed": "cd backend && source venv/bin/activate && python -m scripts.seed_data",
    "test:backend": "cd backend && source venv/bin/activate && python -m pytest ../tests/ -v",
    "test:frontend": "cd frontend && npx playwright test --reporter=list",
    "test": "npm run test:backend && npm run test:frontend"
  },
  "keywords": [
    "gigpulsesentinel",
    "parametric-insurance",
    "gig-workers",
    "ai",
    "guidewire"
  ],
  "author": "TechNova",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
